CONFIG_I2C=y

# 800x480 at RGB565 is larger than the small panels most LVGL samples use.
# Give LVGL a bigger internal memory pool and main stack for widgets.
CONFIG_LV_Z_MEM_POOL_SIZE=65536
CONFIG_MAIN_STACK_SIZE=8192
